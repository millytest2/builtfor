# Copy to .env and fill in. .env is gitignored.

# Google Places API (new Places API v1 / places.googleapis.com).
# Enable "Places API (New)" in Google Cloud, create a key, restrict it.
GOOGLE_PLACES_API_KEY=

# Apollo.io (optional, for owner/contact enrichment in a later phase)
APOLLO_API_KEY=
